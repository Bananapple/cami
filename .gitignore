# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — never commit real credentials
# use .env.example as the template
.env
.env.*
!.env.example

# Supabase CLI temp files
supabase/.temp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.gstack/

# Claude Code worktree scratch space — never tracked in this repo
.claude/worktrees/

# Agent / skill manager state — per-machine, never commit
.agents/
.context/
.hermes/
skills-lock.json
